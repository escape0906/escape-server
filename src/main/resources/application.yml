spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/escape

  jpa:
    show-sql: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
server:
  servlet:
    context-path: /api